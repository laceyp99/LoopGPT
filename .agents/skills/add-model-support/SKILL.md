---
name: add-model-support
description: Add support for a newly announced model from an existing Conductor Core provider. Use when researching official model identifiers, pricing, limits, and parameter support; updating the packaged model registry; verifying provider compatibility; and checking downstream client or evaluation behavior.
---

# Add Model Support

Read and follow the canonical Core-owned skill at
`packages/conductor-core/.agents/skills/add-model-support/SKILL.md`, including
its referenced repo-integration notes.

This root shim exists only so the skill remains discoverable while Conductor is
a monorepo. Remove it when `packages/conductor-core` becomes its own repository
root.
